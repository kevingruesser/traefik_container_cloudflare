---
galaxy_info:
  role_name: deploy_container_traefik_cloudflare
  author: Kevin Heyer
  description: >
    An Ansible role that automates the deployment of Traefik with Docker Compose,
    including directory setup, templated configuration files, and Cloudflare DNS-01
    support for automated Let's Encrypt certificate provisioning.
  company: Skull-IT
  license: MIT

  min_ansible_version: 2.14

  platforms:
    - name: Debian
      versions:
        - bullseye
        - bookworm
        - trixie

  galaxy_tags:
    - traefik
    - docker
    - reverseproxy
    - ansible
    - compose

dependencies:
  - role: geerlingguy.docker
